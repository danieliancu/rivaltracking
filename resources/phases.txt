Etapa 1 — Visual clone
React → Django templates. Totul mock. Aspectul trebuie să fie aproape identic.

Etapa 2 — Product foundation
PostgreSQL, users, workspaces, competitors, products, history, permissions.

Etapa 3 — Intelligence engine
Celery + Redis + scraping workers + matching + AI + alerts + reports.

Etapa 3.5 — Own Catalogue & Real Comparisons
Connect own website/CSV → import catalogue → competitor discovery → own-vs-competitor matching → real price/stock/catalogue-gap comparisons → end-to-end user flow.

Etapa 4 — Commercial launch
Stripe + plans/limits + onboarding + transactional emails + production deployment + backups/monitoring + GDPR/legal + marketing site.