{
  "name": "rivaltracking-app",
  "private": true,
  "description": "Tailwind CSS build for the RivalTracking Django application. Node is a build-time dependency only; the built static/css/app.css is committed.",
  "scripts": {
    "dev": "tailwindcss -i static/src/styles.css -o static/css/app.css --watch",
    "build": "tailwindcss -i static/src/styles.css -o static/css/app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "lucide-static": "0.468.0",
    "tailwindcss": "^4.1.0",
    "tw-animate-css": "^1.4.0"
  }
}
